const mysql = require("mysql");
const inquirer = require("inquirer");
const consoleTable = require("console.table");
const connection = require("../db/connection");

function viewEmployeeDept() {
  //Create a connection that selects from the department

    //create an empty array

    //loop through response and push the response to the empty array

  //propt user for the dept they'd like to view

    //then statement

    //select employee info, join role and department

    //create connection

      //loop through response

      //call mainApp()
}

module.exports = viewEmployeeDept;